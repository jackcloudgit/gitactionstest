name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run Sample Action
        run: |
          cd $GITHUB_WORKSPACE/github/actions/sample-action
          echo "Hello, ${{ inputs.name }} from GitHub Actions!"     
