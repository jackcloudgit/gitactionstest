name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run Sample Action
        uses: ./gitactionstest/github/actions/sample-action
        with:
          name: "Jack"     
